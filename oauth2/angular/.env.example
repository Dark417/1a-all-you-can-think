# Angular reads its config from src/environment.ts, not from .env.
# This file exists only to mirror the other two apps: the value that matters is
#
#   apiBaseUrl = 'http://localhost:8080'
#
# in src/environment.ts.
